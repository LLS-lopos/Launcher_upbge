name: Test GitAction
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
    Explore-GitHub-Actions:
        runs-on: ubuntu-latest
        steps:
            - run: echo "🎉 Le travail a été automatiquement déclenché ${{ github.event_name }}."
            - run: echo "🐧 Ce travail s'exécute désormais sur un serveur ${{ runner.os }} hébergé par GitHub!"
            - run: echo "🔎 Le nom de votre branche est ${{ github.ref }} et votre référentiel est ${{ github.repository }}."
            - name: Check out repository code
              uses: actions/checkout@v5
            - run: echo "💡 Le référentiel ${{ github.repository }} à été cloné."
            - run: echo "🖥️ Le workflow est maintenant prêt à tester votre code sur le coureur."
            - name: List files in the repository
              run: |
                ls ${{ github.workspace }}
            - run: echo "🖥️ The workflow is now ready to test your code on the runner."